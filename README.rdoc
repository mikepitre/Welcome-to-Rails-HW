# Welcome to Rails HW Assignment

Rails version 4.2.1

This rails app contains a users table. The controller directs URL requests and returns a user(s) from the table that meets the parameters stated in the url.
